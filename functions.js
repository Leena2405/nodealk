const helloWorld = () => console.log("New hello World");
const add = (a,b) => console.log(a+b)
const subtitle = "zajecia backed"
const sample = () =>'proba funkcji';

module.exports ={
    helloWorld,
    add,
    subtitle,
    sample
}

console.log(" functions.js loaded correctly");
